@model IEnumerable<Entities.Models.RecipeOccasion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Occasion.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Recipe.Notes)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Occasion.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Recipe.Notes)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RecipeOccasionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RecipeOccasionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RecipeOccasionId })
        </td>
    </tr>
}

</table>
